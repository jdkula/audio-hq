table:
  schema: public
  name: event
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - invalidate
        - time
        - workspace_id
      filter: {}
      allow_aggregations: true
