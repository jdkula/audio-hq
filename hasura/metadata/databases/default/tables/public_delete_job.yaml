table:
  name: delete_job
  schema: public
object_relationships:
  - name: file
    using:
      foreign_key_constraint_on: file_id
insert_permissions:
  - role: anonymous
    permission:
      check: {}
      columns:
        - file_id
select_permissions:
  - role: worker
    permission:
      columns:
        - assigned_worker
        - file_id
        - id
      filter: {}
update_permissions:
  - role: worker
    permission:
      columns:
        - assigned_worker
      filter: {}
      check: null
delete_permissions:
  - role: worker
    permission:
      backend_only: false
      filter: {}
