table:
  schema: public
  name: main_status
object_relationships:
  - name: workspace
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: workspace
        insertion_order: null
        column_mapping:
          workspace_id: id
array_relationships:
  - name: queue
    using:
      manual_configuration:
        remote_table:
          schema: public
          name: play_queue
        insertion_order: null
        column_mapping:
          id: status_id
