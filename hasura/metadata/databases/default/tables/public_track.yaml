table:
  name: track
  schema: public
object_relationships:
  - name: deck
    using:
      foreign_key_constraint_on: deck_id
  - name: single
    using:
      foreign_key_constraint_on: single_id
insert_permissions:
  - role: anonymous
    permission:
      check: {}
      columns:
        - deck_id
        - single_id
        - id
        - ordering
select_permissions:
  - role: anonymous
    permission:
      columns:
        - created_at
        - deck_id
        - single_id
        - ordering
        - id
      filter: {}
delete_permissions:
  - role: anonymous
    permission:
      backend_only: false
      filter: {}
