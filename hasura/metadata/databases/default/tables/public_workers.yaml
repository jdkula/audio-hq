table:
  name: workers
  schema: public
array_relationships:
  - name: delete_jobs
    using:
      foreign_key_constraint_on:
        column: assigned_worker
        table:
          name: delete_job
          schema: public
  - name: jobs
    using:
      foreign_key_constraint_on:
        column: assigned_worker
        table:
          name: job
          schema: public
insert_permissions:
  - role: worker
    permission:
      check: {}
      columns:
        - checkin_frequency_s
        - id
        - last_check_in
        - worker_start
select_permissions:
  - role: worker
    permission:
      columns:
        - checkin_frequency_s
        - id
        - last_check_in
        - worker_start
      filter: {}
update_permissions:
  - role: worker
    permission:
      columns:
        - checkin_frequency_s
        - last_check_in
        - worker_start
      filter: {}
      check: {}
delete_permissions:
  - role: worker
    permission:
      backend_only: false
      filter: {}
